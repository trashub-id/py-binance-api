version: '3.8'

services:
  bot:
    build: .
    container_name: py-binance-bot
    restart: always
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Remove volume mapping logic above in production if you don't want live changes
